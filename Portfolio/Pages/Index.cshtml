@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}
<section class="hero">
	<div class="text-center hero-content">
	    <h1 class="display-4">@Model.Name - Portfolio | @Model.Role</h1>
	    <p>I am building modern applications with .NET, C#, and web technologies.</p>
		<h3> My Projects </h3>

		<div class="d-flex flex-wrap justify-content-center gap-4">
		@foreach (var project in Model.Projects)
		{
			<partial name="_ProjectCard" model="project" />
		}
		</div>
	</div>
</section>